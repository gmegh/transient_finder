description: |
    Pipeline that runs n single_visit_stars and generates catalogs
    of transient candidates per detector, and adds metadata to the
    transient catalogs.
instrument: lsst.obs.lsst.LsstCam
tasks:
  findTransients:
    class: lsst.transient.finder.find_transients.FindTransientsTask
  